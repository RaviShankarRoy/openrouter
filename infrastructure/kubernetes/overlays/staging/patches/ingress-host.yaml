apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openrouter-api
spec:
  tls:
    - hosts:
        - api.staging.openrouter.example
      secretName: openrouter-api-tls
  rules:
    - host: api.staging.openrouter.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway-go
                port:
                  name: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openrouter-app
spec:
  tls:
    - hosts:
        - app.staging.openrouter.example
        - docs.staging.openrouter.example
      secretName: openrouter-web-tls
  rules:
    - host: app.staging.openrouter.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-web
                port:
                  name: http
    - host: docs.staging.openrouter.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-web
                port:
                  name: http
