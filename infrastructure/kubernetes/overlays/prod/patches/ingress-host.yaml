apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openrouter-api
spec:
  tls:
    - hosts:
        - api.openrouter.example
      secretName: openrouter-api-tls
  rules:
    - host: api.openrouter.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway-go
                port:
                  name: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openrouter-app
spec:
  tls:
    - hosts:
        - app.openrouter.example
        - docs.openrouter.example
      secretName: openrouter-web-tls
  rules:
    - host: app.openrouter.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-web
                port:
                  name: http
    - host: docs.openrouter.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-web
                port:
                  name: http
